<form method="POST" class="grid">
    <div class="contact__info">
        <label for="name">
            Votre nom (obligatoire) :
            <input type="text" name="username" id="name" autocomplete="name" required />
        </label>
    </div>
    
    <div class="contact__inputs grid">
        <div class="contact__info">
            <label for="email">
                Votre email (obligatoire) :
                <input type="email" name="useremail" id="email" autocomplete="email" required />
            </label>
        </div>
        
        <div class="contact__info">
            <label for="tel">
                votre téléphone (optionnel) :
                <input type="tel" name="userphone" id="tel" autocomplete="tel" />
            </label>
        </div>
    </div>

    <div class="contact__info">
        <label for="message">
            Votre message (obligatoire) :
            <textarea name="usermessage" id="message" cols="0" rows="7" required
            ></textarea>
        </label>
    </div>

    <div>
        <button type="submit">
            Envoyer
            <i class="uil-message button__icon" aria-hidden="true"></i>
        </button>
    </div>
</form>

<style>
    .contact__info {
        border-radius: .5rem;
        padding: .75rem 1rem .25rem;
        background-color: var(--bg-colored);
    }
    label {
        font-size: var(--smaller-font-size);
    }

    input,
    textarea {
        width: 100%;
        font-family: var(--body-font);
        font-size: var(--normal-font-size);
        border: none;
        outline: none;
        padding: .25rem .5rem .5rem 0;
        background-color: var(--bg-colored);
    }

    textarea {
        resize: vertical;
    }

    button {
        display: inline-flex;
        align-items: center;
        background: linear-gradient(to right, var(--secondary-color-lt) 50%, var(--secondary-color) 50%);
        background-size: 202% 100%;
        background-position: right bottom;
        transition: all .5s ease-out;
        padding: .96em;
        border-radius: .5rem;
    }

    button:hover,
    button:focus {
        background-position: left bottom;
    }

    button > i {
        font-size: 1.25rem;
        margin-left: var(--mar-0-5);
    }

    /* For extra-large devices */
    @media screen and (min-width: 1024px) {
        .contact__form {
            width: 460px;
        }
        .contact__inputs {
            grid-template-columns: 1fr 1fr;
        }
    }
</style>
