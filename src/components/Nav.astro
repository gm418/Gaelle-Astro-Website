---
// data import
import navData from "../data/navData";
---

<nav class="nav container">
    <a href="#">Gaëlle Morin</a>

    <div>
        <ul>
            {
                navData.map(item => (
                    <li>
                        <a href={item.url}>
                            <i class={'uil-' + item.icon}></i>
                            {item.title}    
                        </a>
                    </li>
                ))
            }
        </ul>
        <i class="uil-times"></i>
    </div>

    <div class="nav__btns">
        <!-- Theme change button -->
        <i class="uil-moon"></i>

        <div>
            <i class="uil-apps"></i>
        </div>
    </div>
</nav>

