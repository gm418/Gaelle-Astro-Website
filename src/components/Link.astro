---
const {
href,
classes,
text,
leftIcon,
rightIcon,
iconClasses,
...rest
} = Astro.props;
---

<a
    href={href}
    class={classes}
    {...rest}
>
{
    leftIcon && (
        <i class={'uil-' + leftIcon + (iconClasses ? ' ' + iconClasses : '')}></i>
    )
}
{text}
{
    rightIcon && (
        <i class={'uil-' + rightIcon + (iconClasses ? ' ' + iconClasses : '')}></i>
    )
}
</a>


<style>
    .primary {
        color: var(--title-color);
    }

    .secondary {
        color: #FFF;
    }

    .underline {
        text-decoration: underline;
    }

    .filled {
        display: inline-flex;
        align-items: center;
        background-color: red;
        color: #fff;
        padding: 1em;
        border-radius: .5rem;
    }

    /*========== classes ==========*/
    .nav__link {
        display: flex;
        flex-direction: column;
        align-items: center;
        font-size: var(--small-font-size);
        word-break: break-all;
    }

    .nav__link:hover {
        color: var(--title-color-light);
    }

    /*========== iconClasses ==========*/
    .nav__icon {
        font-size: 1.28em;
    }
</style>
